# NLP
stanza>=1.8.0
pymorphy3>=0.9.1              # лемматизация для стратификации
navec>=0.10.0                 # компактные предобученные векторы (Natasha / Яндекс, ~50 МБ модель)

# GUI
PyQt6>=6.6.0
matplotlib>=3.8.0

# AI-детектор GigaCheck
transformers>=4.41.0,<6.0.0
torch>=2.0.0

# ThemeEngineV2 shadow: optional/lazy, модель используется только из local cache
sentence-transformers>=5.7.0,<6.0.0

# Экспорт
python-docx>=1.0.0
openpyxl>=3.1.0

# ML / числа
scikit-learn>=1.3.0
numpy>=1.24.0
PyYAML>=6.0                # версионируемые профили экспертных методик
cryptography>=42.0         # AES-256-GCM для контейнера .avedcase
argon2-cffi>=23.1          # Argon2id для получения ключа из пароля

# Итерация 1: кэш + история
diskcache>=5.6.0       # кэш NLP-результатов на диске (опционально, fallback на memory)

# Итерация 2: проверка ошибок
language-tool-python>=2.7.1   # требует Java 8+ для локального сервера (fallback на API)

# Итерация 3: быстрый NLP-бэкенд
spacy>=3.7.0                   # + python -m spacy download ru_core_news_sm (или lg/md)

# Итерация 4: самообучение
gensim>=4.3.0                  # FastText + LDA (инкрементальное обучение)
